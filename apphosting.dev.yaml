# Environment-specific overrides for the 'dev' environment. (DIAGNOSTIC STEP)

env:
  # Temporarily using a plaintext value to diagnose the Secret Manager issue.
  - variable: FIREBASE_PROJECT_ID
    value: "studio-535179390-fbbb5"

  # We still expect the build to fail on the next secret, which will prove the issue is with Secret Manager access.
  - variable: FIREBASE_CLIENT_EMAIL
    secret: firebase-client-email
  - variable: FIREBASE_PRIVATE_KEY
    secret: firebase-private-key
